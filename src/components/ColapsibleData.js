import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const ColapsibleData = ({itemTilte}) => {
  return (
    <View>
      <Text>{itemTilte}</Text>
    </View>
  )
}

export default ColapsibleData

const styles = StyleSheet.create({})